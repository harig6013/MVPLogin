import { EmployeeAttendance } from './employee-attendance';

describe('EmployeeAttendance', () => {
  it('should create an instance', () => {
    expect(new EmployeeAttendance()).toBeTruthy();
  });
});
